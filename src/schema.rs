table! {
    users (id) {
        id -> Int4,
        name -> Nullable<Varchar>,
        username -> Varchar,
        email -> Varchar,
        phone -> Nullable<Varchar>,
        website -> Nullable<Varchar>,
    }
}
